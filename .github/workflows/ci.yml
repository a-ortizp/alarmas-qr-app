# CI de ambas apps · copiar a .github/workflows/ci.yml
# Ejecuta solo lo que cambió: Gradle para apps/movil, npm para apps/web.
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cambios:
    runs-on: ubuntu-latest
    outputs:
      movil: ${{ steps.filtro.outputs.movil }}
      web: ${{ steps.filtro.outputs.web }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filtro
        with:
          filters: |
            movil:
              - 'apps/movil/**'
              - 'packages/tokens/**'
            web:
              - 'apps/web/**'
              - 'packages/tokens/**'

  movil:
    needs: cambios
    if: needs.cambios.outputs.movil == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/movil
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools platforms;android-36 build-tools;36.0.0'
      - uses: gradle/actions/setup-gradle@v4
      - run: chmod +x gradlew
      - name: Lint y pruebas unitarias
        run: ./gradlew lintDebug testDebugUnitTest --no-daemon
      - name: Compilar APK de depuración
        run: ./gradlew assembleDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: apps/movil/app/build/outputs/apk/debug/*.apk
          retention-days: 7

  web:
    needs: cambios
    if: needs.cambios.outputs.web == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.23.2'
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - run: npm ci
      - name: Lint
        run: npm run lint --if-present
      - name: Pruebas
        run: npx ng test --watch=false
      - name: Build de producción
        run: npx ng build --configuration production
